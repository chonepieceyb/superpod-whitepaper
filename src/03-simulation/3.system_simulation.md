# 系统模型：把需求与供给映射为可推演行为

如果说负载模型定义了需求侧压力，资源模型定义了供给侧边界，那么系统模型的职责就是把两者连接起来，形成真正可推演的**系统行为（System Behavior）**。第三章中的系统模型不是附属说明，而是整条仿真链的核心引擎：它负责**映射（Mapping）**与**编排（Scheduling）**，把“需求是什么”与“供给能做到什么”转化为“系统最终会怎样运行”。

它要回答的不是抽象的“快还是慢”，而是：在给定工作负载、资源画像与并行/调度策略时，系统会表现出怎样的吞吐、时延、利用率、显存曲线、拥塞轨迹和长尾行为。

系统模型的工作可以概括为两步：
1. **空间映射**：把逻辑负载实例化到具体物理资源上。
2. **时间编排**：在离散时间轴上推演计算、通信、存储和同步行为如何交错发生。

## 映射空间与原语转换（Mapping & Lowering）

映射空间定义了从逻辑域（Logical Domain）到物理域（Physical Domain）的转换规则。该阶段作为仿真推演的前置静态配置环节，旨在完成计算图的**物理实例化（Physical Instantiation）**与通信原语的**底层展开（Primitive Expansion）**。

### 逻辑-物理映射（Logical-Physical Mapping）

本小节定义逻辑计算节点 $V_{logical}$ 与物理设备集合 $D_{physical}$ 之间的拓扑绑定关系，确立仿真的空间坐标系。

* **算子布局（Operator Placement）**
    建立逻辑设备 ID 到物理集群拓扑坐标的映射函数。仿真引擎维护全局**设备映射表（Device Map）**，将逻辑 `Rank` 映射为物理三元组 `(Rack_ID, Node_ID, Accelerator_ID)`。该映射关系直接决定了后续通信链路的物理属性（如 NVLink、PCIe 或 RDMA 网络）及相应的延迟与带宽约束。

* **并行策略实例化（Parallelism Instantiation）**
    依据用户定义的分布式并行策略（TP/PP/DP），对原始计算图执行**图变换（Graph Transformation）**，生成适配物理集群规模的执行图：
    * **张量切分（Tensor Tiling）**：针对张量并行（TP）场景，根据切分度数 $N$，将单体算子（如 MatMul）分解为 $N$ 个子核（Sub-kernels），并建立子核与物理设备的绑定关系。
    * **逻辑复制（Logical Replication）**：针对数据并行（DP）场景，在多个设备组间克隆模型参数与计算子图，构建并行执行副本。
    * **胶水节点插入（Glue Node Insertion）**：在切分后的子图边界自动生成必要的通信原语节点（如 `AllReduce`、`AllGather` 或 `P2P_Send/Recv`），以保证变换后的计算图在语义上的等价性。

### 通信原语降级（Communication Lowering）

负载模型中定义的通信语义（如“全局梯度聚合”）属于高层逻辑意图。系统层须依据物理网络拓扑与集合通信库（CCL）的具体实现，将高层意图**降级（Lowering）**为微观传输指令。

* **拓扑感知算法展开（Topology-aware Algorithm Expansion）**
    集合通信操作的性能表现取决于底层算法与物理拓扑的匹配度。系统层内置**集合通信算法库**，执行如下展开逻辑：
    * **算法选择**：依据物理连接关系（如 Ring, Mesh, Torus, Fat-tree），选择更适配的通信算法（例如：Ring-AllReduce、Tree-AllReduce 或 Hierarchical-AllReduce）。
    * **原语分解**：将集合操作分解为点对点（P2P）传输序列。例如，在 $N$ 节点的 Ring-AllReduce 算法中，将逻辑操作展开为 $2(N-1)$ 个 stage 的 `Send`/`Recv` 指令对；实际实现中可通过分块与流水化机制实现阶段级重叠，以刻画计算与通信的并发行为。

* **传输分块机制（Transmission Chunking）**
    为精确模拟大规模训练中的**计算-通信重叠（Communication-Computation Overlap）**特性，系统层引入数据分块策略。将大粒度传输张量切分为微块（Chunks），并在依赖图中建立细粒度的“块级依赖”。这使得仿真器能够捕捉到流水线执行中的微观并发行为，而非仅计算宏观总线带宽。

### 资源仲裁配置（Resource Arbitration Config）

物理环境中的计算单元、内存总线与网络接口均为竞争性资源。映射阶段需预定义资源争抢时的仲裁策略与流控规则。

* **流与队列映射（Stream & Queue Mapping）**
    定义逻辑执行流（Execution Stream）与硬件工作队列（Hardware Work Queues）的对应关系：
    * **通道隔离与并发**：配置计算流、H2D/D2H 拷贝流与 P2P 通信流在 DMA 引擎上的映射策略，确立不同类型指令在硬件层面的并发执行能力与互斥约束。
    * **优先级配置**：设定不同硬件队列的调度优先级（Priority Level），以模拟高优先级控制流对计算资源的抢占行为。

* **协议开销建模（Protocol Overhead Modeling）**
    除数据传输时延外，系统层引入对通信协议栈开销的量化建模：
    * **连接建立与同步延迟**：模拟跨节点通信的连接建立、控制面协商（如信用交换、队列初始化）等固定开销，以及逻辑屏障（Barrier）的同步等待时间。
    * **软件栈延迟**：量化内核态/用户态上下文切换、驱动层队列管理带来的固定指令周期开销（Latency Overhead）。

### 系统状态变量：仿真到底在推进什么

如果系统模型只是把算子时间和链路时间相加，它就还不是“系统模型”，而只是一个性能估算器。真正的系统模型必须显式维护一组能够随时间演化的**系统状态变量**，因为决定结果的往往不是单次执行时间，而是这些状态如何在时间轴上相互作用。

| 状态变量 | 典型内容 | 对结果的决定作用 |
|:---------|:---------|:-----------------|
| **队列状态** | 算子等待队列、通信队列、优先级队列长度 | 决定排队延迟与重叠是否成立 |
| **链路状态** | 平面占用率、拥塞程度、重传与抖动水平 | 决定通信是否出现长尾与放大 |
| **存储状态** | HBM 占用、KV 生命周期、碎片率、换入换出压力 | 决定显存峰值、驱逐代价与可行域 |
| **调度状态** | 已分配资源、待重平衡对象、调度器时钟与决策周期 | 决定理论资源何时能转化为有效执行 |
| **恢复状态** | 故障域、已触发恢复路径、恢复剩余时间 | 决定系统在扰动后何时回到稳态 |

把这些状态变量显式化有一个直接好处：第三章后面讨论的 Goodput、长尾、恢复损失和工程可达前沿，都可以被追溯到某一种状态演化，而不是只停留在结果层的“快了/慢了”[^ultra-report-system-state]。

## 训练场景仿真：同步流水线编排

训练负载具有典型的**强同步、周期性、批处理（Synchronous, Periodic, Batch-oriented）**特征。其执行过程以迭代（Iteration/Step）为基本单位，在并行切分（TP/PP/DP）与一致性约束的共同作用下形成稳定的时序结构。相较于静态计算图分析，训练场景下的系统效率主要由同步传播、资源竞争与空转行为所决定，因此需引入面向时间轴的编排建模，用于刻画吞吐、利用率及长尾性能的形成机制。

### 训练执行流的时序结构：迭代系统与同步约束

训练执行流可视为受一致性约束驱动的迭代系统，其时序特征主要体现在：

* **迭代边界（Iteration Boundary）**
    每一轮训练存在清晰的逻辑边界，迭代边界与同步点共同决定训练的吞吐上界及稳态特征。

* **一致性约束（Consistency Constraint）**
    梯度一致性与参数一致性要求引入多级同步语义（如梯度聚合与更新边界），导致训练系统呈现“慢者决定整体”的全局耦合特征。

* **多尺度耦合（Multi-scale Coupling）**
    训练时序同时受算子依赖、跨 Stage 因果依赖、跨 Replica 同步依赖共同约束，形成多层嵌套的依赖结构与同步传播路径。

### 流水线编排的目标：将同步系统逼近稳态吞吐上界

在固定负载需求与物理资源约束下，训练编排的核心目标是最大化稳态有效工作时间，并压缩同步等待与空转开销。

* **稳态吞吐（Steady-state Throughput）**
    训练系统的主要优化对象是迭代吞吐而非单次延迟；吞吐由同步点、资源占用连续性与调度扰动共同决定。

* **有效工作占比（Effective Work Ratio）**
    流水线编排关注“设备在时间轴上的有效工作占比”，其损失主要表现为流水线空转与同步等待。

* **策略差异的统一刻画（Unified Characterization）**
    典型编排策略（如 1F1B、Interleaved、ZB1P）可在统一框架下比较：它们的本质差异体现为依赖结构松紧、粒度设置与稳态占用率的不同取舍。

### 关键现象：气泡、同步放大与重叠衰减

训练系统中可观测的性能损失通常表现为三类典型现象：

* **流水线气泡（Pipeline Bubble）**
    气泡体现为空转区间，可由结构性依赖、阶段负载不均衡与资源争抢等多种因素共同触发。

* **同步放大效应（Straggler Amplification）**
    在强同步体系下，局部拖尾会放大为全局迭代时间的增长，进而影响稳态吞吐与性能稳定性。

* **重叠有效性衰减（Overlap Degradation）**
    计算-通信重叠存在理论上限，但实际有效性将受到队列竞争、链路瓶颈与协议开销抑制，导致“可重叠”与“实际重叠”之间出现系统性差距。

* **平面干扰传播（Cross-plane Interference Propagation）**
    当参数面、数据面与业务面的资源隔离不足时，原本局部的排队与带宽争用会沿调度路径传播，最终以同步拖尾、吞吐下降或阶段性抖动的形式表现出来。对大规模训练来说，这种传播常常比单次算子波动更值得建模[^ultra-report-system-planes]。

### 训练显存行为：峰值、生命周期与可行性边界

训练显存不仅是静态预算，更是由生命周期与同步边界共同塑造的动态资源曲线。

* **峰值显存与出现位置（Peak Memory & Where It Happens）**
    激活值、梯度与优化器状态的生命周期交织决定峰值出现位置；峰值显存约束将直接影响并行策略可行空间与吞吐上界。

* **生命周期锁定效应（Lifecycle Lock-in）**
    同步点将部分存储对象在特定阶段“锁定”在显存中，形成训练场景特有的周期性压力模式。

* **策略代价曲线（Trade-off Curve）**
    重计算、卸载等策略可在显存压力与算力/带宽压力之间交换，其价值体现在对吞吐上界与峰值约束的重新塑形。

### 评价维度：吞吐、效率与可解释性

训练编排的评价应同时覆盖性能结果与系统行为两类维度：

* **吞吐与效率（Throughput & Efficiency）**
    - Step Time / Throughput
    - Pipeline Efficiency（有效工作时间占比）
    - Synchronization Penalty（同步等待占比）

* **行为可解释性（Interpretability）**
    - Bubble 分解：结构性 / 负载不均衡 / 资源竞争
    - 重叠有效性：理论上限 vs 实际达成
    - 拖尾诊断：关键路径与同步传播

* **可扩展性与稳定性（Scalability & Stability）**
    - 并行度扩展下的效率变化趋势
    - 对系统扰动（网络抖动、算子波动）的敏感性
    - 异常工况下的退化模式

### 恢复路径：从故障发生到稳态回归

训练场景里的系统模型还应显式定义“故障之后会发生什么”。在大规模系统中，故障并不是单次离群事件，而更像持续背景噪声，因此恢复路径本身就是吞吐模型的一部分。一个最小可用的恢复状态机至少应覆盖：

1. **检测阶段**：故障何时被发现，检测延迟是多少。
2. **隔离阶段**：影响被限制在链路、节点、Pod 还是整作业。
3. **恢复阶段**：触发重试、重路由、节点摘除、作业回滚还是 checkpoint 恢复。
4. **回稳阶段**：系统需要多久才能从恢复态重新回到稳态吞吐。

如果不把这条路径纳入系统模型，那么所有训练吞吐结果都默认建立在“系统始终处于健康稳态”这一过强假设上，而这恰恰是超大规模集群最不成立的前提之一[^ultra-report-system-recovery]。

## 推理场景仿真：异步事件驱动编排

推理系统与训练系统的根本差异在于：它不是围绕一个强同步的迭代边界展开，而是围绕持续到达的请求流、动态批处理和时延约束展开。系统模型在推理侧的任务，不是只估算单个 token 的执行时间，而是解释**请求到达、批处理拼接、KV 管理、通信等待和尾延迟**如何共同决定 TTFT、TPOT 与系统级 Goodput。

### 请求流与调度入口

推理模型首先要刻画的，是工作负载如何进入系统：

* **到达过程（Arrival Process）**
    请求可能来自 Poisson 流、突发流、业务 Trace 重放或多租户混部环境。系统模型需要显式表达请求间隔、长度分布与优先级，而不是假设一个恒定输入速率。

* **调度入口（Scheduling Entry）**
    Continuous batching、抢占、优先级队列和 admission control 决定请求何时进入执行面。它们直接影响队列长度、TTFT 和系统吞吐之间的 trade-off。

推理侧尤其需要把调度器看成系统状态的一部分，而不是外部给定条件。因为在高并发、短时长、强实时场景下，很多性能退化并不是来自单次算子慢，而是来自调度反应速度跟不上请求结构变化：该合批的时候没有及时合批，该重平衡的时候没有及时重平衡，该限流的时候没有及时限流。系统模型若不模拟这种“调度滞后”，就很难解释为什么理论吞吐充足但线上体验依然波动[^ultra-report-system-infer]。

### 阶段性执行流：Prefill 与 Decode

推理负载并不是均匀的。系统模型需要把不同阶段的瓶颈拆开建模：

* **Prefill 阶段**
    Prompt 处理阶段通常计算密集，更接近算力上界，对 batch 合并和算子吞吐更敏感。

* **Decode 阶段**
    逐 token 生成阶段更依赖访存与 KV 命中率，常常由 HBM 带宽、分页代价和调度等待共同决定尾延迟。

* **阶段切换成本**
    Prefill 到 Decode 的切换、跨节点状态同步与批次重组，往往决定了“理论 tokens/s”能否真正转化成用户感知速度。

### KV Cache 与显存行为

推理侧的系统模型必须显式纳入 KV Cache 的生命周期管理，因为很多架构收益与失效都发生在这里：

* **分页与碎片化**
    PagedAttention、块分配策略和内存碎片会影响显存占用上界与长期稳态性能。

* **交换与驱逐**
    当显存不足时，swap-out/swap-in 和 block eviction 会放大尾延迟，并改变可达前沿的位置。

* **混部与干扰**
    多模型、多租户或长短请求混部时，KV 行为往往成为系统级抖动与不公平性的主要来源。

### 推理侧的可建模控制变量

为了让推理仿真不退化成“换一组 batch 再跑一次”，系统模型至少应把以下控制变量开放出来：

| 控制变量 | 典型取值 | 主要影响 |
|:---------|:---------|:---------|
| **请求到达模型** | Poisson、突发流、Trace 回放 | 决定队列形态与 TTFT 波动 |
| **合批策略** | 静态批、连续批、优先级批 | 决定吞吐与尾延迟权衡 |
| **阶段映射策略** | 聚合、`PD` 分离、异构 `PD` | 决定 Prefill/Decode 失配程度 |
| **KV 管理策略** | 驻留、分页、分层、驱逐 | 决定显存边界与 TPOT 长尾 |
| **重平衡/限流策略** | 阈值触发、周期触发、SLA 触发 | 决定高峰期是否进入不稳定区 |

这些变量之所以重要，是因为它们共同决定“理论前沿”与“工程可达前沿”之间的距离；很多时候，前沿并不是被硬件上限卡住，而是被调度和缓存策略卡住[^ultra-report-system-infer]。

### 推理场景的评价维度

推理侧最关键的输出通常包括：

- **TTFT / TPOT / P95 / P99**：描述用户感知时延。
- **tokens/s/GPU 与 tokens/s/MW**：描述单位资源效率。
- **队列等待与拒绝率**：描述系统在 SLA 约束下的可服务能力。
- **显存占用与 KV 命中/驱逐行为**：解释系统是否处于稳定工作区间。

## 系统模型的输出：从执行轨迹到决策指标

系统模型的目标不是只给出一个“更快/更慢”的结论，而是输出一组既能被校准、又能被后续章节消费的结果。通常至少包括三类。这里需要一并收束的是：**网络与互联能力在系统模型中的价值，最终必须通过 GPU 利用率、Goodput、训练迭代效率、推理 TTFT/TPOT 长尾与恢复代价来呈现。** 如果仿真输出仍主要停留在端口带宽、单跳时延或单次 collective 时间，那么它只能说明局部子系统的表现，不能说明超节点是否真的把能力兑现为了可交付结果。

| 输出层级 | 典型结果 | 主要用途 |
|:---------|:---------|:---------|
| **时序层输出** | 执行轨迹、队列状态、链路占用、显存曲线 | 用于解释瓶颈与长尾来源 |
| **指标层输出** | 吞吐、时延、利用率、Goodput、能效 | 用于比较不同策略与方案 |
| **结构层输出** | 瓶颈归因、同步放大、重叠衰减、敏感性结果 | 用于支撑第四章和第五章的判断 |

这三类输出的关系很重要：没有时序层输出，指标层只是一组分数；没有结构层输出，后续章节就无法解释“为什么这个方案更靠近前沿”。

## 对第三章主线的作用

在新的章节叙事里，系统模型是最核心的中间环节：

- 它接收负载模型的需求规约。
- 它调用资源模型的供给参数。
- 它把两者转化为可校准的系统行为。
- 它再把这些行为交给后文的校准验证、输出接口与共演进闭环。

也正因为如此，第三章里的评测不应写成系统模型的主体。系统模型负责产生可解释的推演结果，评测只负责校准这些结果。这样，章节主线才能始终落在“建模”而不是“测评”上。

[^ultra-report-system-state]: 《超大规模智算集群关键技术及工程落地研究报告》, 2025 年 12 月, 第 18-21 页、第 27-28 页。涉及多平面流量、统一调度、资源碎片、故障恢复与长期稳定运行。仓库内文件：`pdf/超大规模智算集群关键技术及工程落地研究报告.pdf`。
[^ultra-report-system-planes]: 《超大规模智算集群关键技术及工程落地研究报告》, 2025 年 12 月, 第 16-19 页。涉及参数面/数据面/业务面分层以及多平面网络组织。仓库内文件：`pdf/超大规模智算集群关键技术及工程落地研究报告.pdf`。
[^ultra-report-system-recovery]: 《超大规模智算集群关键技术及工程落地研究报告》, 2025 年 12 月, 第 27-28 页。涉及故障频发、智能容错、快速恢复与长期运行稳定性。仓库内文件：`pdf/超大规模智算集群关键技术及工程落地研究报告.pdf`。
[^ultra-report-system-infer]: 《超大规模智算集群关键技术及工程落地研究报告》, 2025 年 12 月, 第 27 页、第 30-31 页。涉及推理/智能体业务的高并发、短时长、强实时特点，以及统一调度和资源配置压力。仓库内文件：`pdf/超大规模智算集群关键技术及工程落地研究报告.pdf`。
